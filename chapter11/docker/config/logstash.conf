input {
    tcp {
        port => 5000
        codec => json_lines
    }
}

filter {
    mutate {
        add_tag => [ "murat-tag" ]
    }
}

output {
    elasticsearch {
        hosts => ["elasticsearch:9200"]
    }
}